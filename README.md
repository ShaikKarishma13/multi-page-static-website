# multi-page-static-website


🔹 HTML (HyperText Markup Language)
HTML is used to structure the content of the website.
It defines elements such as:
Headings, paragraphs
Forms and input fields
Links and navigation menus
Each page uses proper HTML tags like <header>, <nav>, <main>, <form>, and <footer>.


🔹 CSS (Cascading Style Sheets)
CSS is used to style and layout the web pages.
In this project, CSS is written using internal style sheets to:
Set fonts and colors
Align content
Design forms and buttons
Improve visual appearance


🔹 Home Page (home.html)
The Home page acts as the main entry point of the website.
It contains:
A header with the website title
A navigation bar linking all pages
A brief description of the website
A fixed footer


🔹 Login Page (login.html)
The Login page provides a user authentication interface.
It includes:
Username and password input fields
A submit button
A link back to the Home page
HTML form elements are used to collect user credentials.


🔹 Catalog Page (catalog.html)
The Catalog page displays a list of products.
Each product is shown inside a styled container with:
Product name
Description and price
This page demonstrates content presentation and layout design using CSS.


🔹 Registration Page (register.html)
The Registration page allows new users to sign up.
It includes:
Full name, email, password, and confirm password fields
A submit button
Navigation link back to Home
HTML5 input types like email and password are used for better validation.
